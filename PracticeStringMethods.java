import java.util.Locale;

@SuppressWarnings("ALL")

public class PracticeStringMethods {

    public static void main(String[] args) {

        // Fjern kommentering for å prøve ut de forskjellige methodsene.

        String name = "Bro";

        //boolean result = name.equals("Bro"); //Gir oss true eller false om result er det samme som name
        //boolean result = name.equalsIgnoreCase("bro"); //Samme som over, men ignorer upper og lower-case

        //int result = name.length(); //Result er = lengded på string. I dette tilfelle 3. (0,1 og 2)

        //char result = name.charAt(0); //Result gir oss charAt posisjon 0. name har i dette tilfelle "B" på posisjon 0.
        //int result = name.indexOf("B"); //Samme som over bare omvendt.

        //boolean result = name.isEmpty(); //Gir oss en boolean value hvis name er tom = (""). True eller false

        //String result = name.toUpperCase();
        //String result = name.toLowerCase();

        //String result = name.trim(); //Fjerner empty-space. Eks ("   Bro    ") --> ("Bro")

        //String result = name.replace('o', 'a'); // Bytter bokstaver

        System.out.println();
    }
}
